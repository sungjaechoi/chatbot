# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# 보안 주의사항:
# - .env 파일에는 API 키 등 민감한 정보가 포함되므로 절대 커밋하지 마세요.
# - 만약 실수로 .env 파일을 커밋한 경우, 다음 단계를 따르세요:
#   1. API 키를 즉시 재발급하세요 (기존 키는 노출된 것으로 간주)
#   2. Git 히스토리에서 제거:
#      git filter-branch --force --index-filter \
#        "git rm --cached --ignore-unmatch .env" \
#        --prune-empty --tag-name-filter cat -- --all
#   3. 강제 푸시 (주의: 팀원과 협의 필요):
#      git push origin --force --all

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
