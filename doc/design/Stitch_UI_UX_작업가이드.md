# Stitch 디자인 → 프로젝트 적용 UI/UX 작업 가이드

> **생성일:** 2026-02-10 (v3 — 설정 패널/디자인 토큰 재확인 반영)
> **소스:** Google Stitch (`stitch_desktop_login_screen.zip`) — 8개 화면
> **대상 프로젝트:** Document Chat (Next.js + Supabase + Tailwind CSS)

---

## 핵심 결정 사항

| 항목 | 결정 |
|------|------|
| **폰트** | Pretendard Variable 유지 (Stitch의 Inter/Manrope 미적용) |
| **백엔드/스키마 변경** | 불필요 — 프론트엔드만 수정 (1건 RPC 파라미터 추가는 선택) |
| **작업 성공률** | 92% (프론트엔드 전용 작업, 상세 평가는 섹션 9 참조) |
| **설정 패널** | 기존 구현 보존 + Stitch 디자인 스타일로 외형만 조정 |

---

## v3 변경 이력

프로젝트에 다음 기능이 추가되어 작업 가이드를 재반영합니다:

| 추가/변경 항목 | 관련 파일 | 작업 가이드 영향 |
|---------------|-----------|-----------------|
| **사용자 설정 패널** (신규) | `ChatSidebar.tsx`, `CollectionListView.tsx` | Stitch 디자인에 없으므로 기존 기능 보존 + 스타일 통합 필요 |
| **회원 탈퇴 모달** (신규) | `AccountDeleteModal.tsx`, `account/deleted/page.tsx` | Stitch 미포함, 기존 구현 유지 |
| **크레딧 잔액 표시** (신규) | `creditsStore.ts`, `useCredits.ts`, `CollectionListView.tsx` | 대시보드 헤더에 잔액 표시 보존 |
| **디자인 토큰 체계화** (변경) | `globals.css` | 기존 토큰이 CSS 변수로 체계화됨 → Stitch 토큰과 병합 방식 변경 |
| **로그인 페이지 리디자인** (변경) | `login/page.tsx` | 이미 부분 적용됨, Stitch와 차이점 재확인 |

---

## 목차

1. [Stitch 디자인 화면 목록](#1-stitch-디자인-화면-목록)
2. [현재 디자인 토큰 현황 및 Stitch 병합 계획](#2-현재-디자인-토큰-현황-및-stitch-병합-계획)
3. [화면별 작업 명세](#3-화면별-작업-명세)
4. [Stitch 미포함 — 기존 기능 보존 목록](#4-stitch-미포함--기존-기능-보존-목록)
5. [수정 대상 파일 매핑](#5-수정-대상-파일-매핑)
6. [작업 순서 및 우선순위](#6-작업-순서-및-우선순위)
7. [주의사항 및 호환성 체크](#7-주의사항-및-호환성-체크)
8. [백엔드 영향 분석](#8-백엔드-영향-분석)
9. [작업 성공률 평가](#9-작업-성공률-평가)

---

## 1. Stitch 디자인 화면 목록

| # | 폴더명 | 화면 | 모드 | 대응 파일 |
|---|--------|------|------|-----------|
| S1 | `desktop_login_screen_5` | 로그인 페이지 | Light | `src/app/login/page.tsx` |
| S2 | `desktop_dashboard_light_mode_1` | 대시보드 (문서 있음) | Light | `src/features/pdf/CollectionListView.tsx` |
| S3 | `desktop_dashboard_light_mode_2` | 대시보드 (빈 상태) | Light | `src/features/home/HomeClient.tsx` |
| S4 | `desktop_login_screen_1` | PDF 업로드 모달 (드래그&드롭) | Dark | `src/features/pdf/PdfUploadModalView.tsx` |
| S5 | `desktop_login_screen_2` | PDF 업로드 모달 (파일 선택됨) | Dark | `src/features/pdf/PdfUploadModalView.tsx` |
| S6 | `desktop_login_screen_3` | PDF 업로드 모달 (진행중) | Dark | `src/features/pdf/PdfUploadView.tsx` |
| S7 | `desktop_login_screen_4` | 업로드 완료 (임베딩 성공) | Dark | `src/features/pdf/PdfUploadView.tsx` |
| S8 | `desktop_chat_dark_mode` | 채팅 페이지 | Light | `src/features/chat/` 전체 |

---

## 2. 현재 디자인 토큰 현황 및 Stitch 병합 계획

> ⚠️ **중요:** 프로젝트의 디자인 토큰이 이미 CSS 변수 체계로 정비되었습니다.
> Stitch 토큰을 무조건 덮어쓰는 것이 아니라, **기존 체계를 유지하면서 Stitch 전용 값만 추가/조정**합니다.

### 2.1 현재 프로젝트 디자인 토큰 (globals.css 기준)

#### 컬러 — 현재 구현 완료

| 토큰 | 현재값 | Stitch 값 | 조치 |
|------|--------|-----------|------|
| `--color-ink` | `#1a1f2e` | `#1a1f2e` | 동일 ✅ 유지 |
| `--color-ink-light` | `#2d3548` | 없음 | ✅ 유지 |
| `--color-ink-muted` | `#6b7280` | 없음 | ✅ 유지 |
| `--color-cream` | `#faf9f7` | `#faf9f7` | 동일 ✅ 유지 |
| `--color-cream-dark` | `#f0eeea` | `#f2f0eb` | 현재값 유지 (미세 차이) |
| `--color-paper` | `#ffffff` | `#ffffff` | 동일 ✅ 유지 |
| `--color-accent` | `#5b7a9d` | `#5b7a9d` | 동일 ✅ 유지 |
| `--color-accent-soft` | `#7a9bbf` | 없음 | ✅ 유지 |
| `--color-accent-glow` | `rgba(91,122,157,0.15)` | 없음 | ✅ 유지 |
| `--color-ai-bg` | `#f8f6f3` | `#f8f6f3` | 동일 ✅ 유지 |
| `--color-ai-border` | `#e8e4de` | `#e5e2da` (Stitch) | 현재값 유지 (미세 차이) |
| `--color-user-bg` | `#1a1f2e` | `#1a1f2e` | 동일 ✅ 유지 |
| `--color-error` | `#b91c1c` | 없음 | ✅ 유지 |
| `--color-error-bg` | `#fef2f2` | 없음 | ✅ 유지 |
| `--color-success` | `#166534` | `#10b981` (Stitch) | ⚠️ 검토 필요 |
| `--color-success-bg` | `#f0fdf4` | 없음 | ✅ 유지 |

#### Stitch 전용 — 추가 필요한 토큰

| 토큰 | 값 | 용도 | 조치 |
|------|-----|------|------|
| `--color-primary` | `#16439c` | Stitch의 주요 버튼/강조색 | 🆕 추가 |
| `--color-primary-hover` | `#11357d` | Primary 호버 | 🆕 추가 |
| `--color-sidebar-bg` | `#f2f0eb` | 채팅 사이드바 배경 | 🆕 추가 |
| `--color-border-light` | `#e5e2da` | Stitch 보더 (채팅 등) | 🆕 추가 |
| `--color-pdf-red` | `#ef4444` | PDF 아이콘 | 🆕 추가 |

#### 그림자 — 현재 구현 완료

| 토큰 | 현재값 | Stitch 차이 | 조치 |
|------|--------|-------------|------|
| `--shadow-sm` | `0 1px 2px rgba(26,31,46,0.04)` | 유사 | ✅ 유지 |
| `--shadow-md` | `0 4px 12px rgba(26,31,46,0.08)` | 유사 | ✅ 유지 |
| `--shadow-lg` | `0 8px 30px rgba(26,31,46,0.12)` | 유사 | ✅ 유지 |
| `--shadow-xl` | `0 20px 50px rgba(26,31,46,0.15)` | 유사 | ✅ 유지 |
| `--shadow-float` | `0 10px 40px ...` | 없음 | ✅ 유지 |

Stitch 전용 그림자 추가:

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--shadow-luxury` | `0 4px 20px rgba(22,67,156,0.04)` | 🆕 대시보드 카드 |
| `--shadow-card-hover` | `0 10px 30px rgba(22,67,156,0.08)` | 🆕 카드 호버 |

#### 간격/반경/트랜지션 — 현재 구현 완료 (변경 없음)

| 체계 | 현재 상태 | 조치 |
|------|-----------|------|
| `--space-*` (xs~2xl) | 6단계 정의 완료 | ✅ 유지 |
| `--radius-*` (sm~xl) | 4단계 정의 완료 (8/12/20/28px) | ✅ 유지 |
| `--transition-*` (fast/base/slow) | 3단계 정의 완료 | ✅ 유지 |

#### 기존 CSS 클래스 — 보존 필수

| 클래스 | 용도 | 사용 위치 |
|--------|------|-----------|
| `.focus-ring` | 포커스 아웃라인 | 전역 (모든 인터랙티브 요소) |
| `.btn-lift` | 버튼 호버 효과 | 전역 버튼 |
| `.animate-float-in` | 목록 아이템 등장 | CollectionListView |
| `.sidebar-root`, `.sidebar-item--*` | 사이드바 스타일 | ChatSidebar |
| `.settings-panel`, `.settings-panel-*` | 설정 패널 | ChatSidebar, CollectionListView |

### 2.2 타이포그래피 변경

#### 폰트 패밀리 — ⚠️ Pretendard 유지 결정

> **결정:** Stitch에서 Inter, Manrope, Noto Sans KR을 사용했으나,
> 기존 프로젝트의 **Pretendard Variable을 그대로 유지**합니다.
> Pretendard는 한국어/영문 모두 커버하며, 추가 폰트 로딩에 따른 성능 저하를 방지합니다.

| 화면 | Stitch 제안 | 실제 적용 | 이유 |
|------|------------|-----------|------|
| 전역 | Inter + Pretendard | **Pretendard Variable 유지** | 한/영 통합, 추가 로딩 없음 |
| 채팅 페이지 | Manrope + Noto Sans KR | **Pretendard Variable 유지** | 일관성 유지, 폰트 3개 추가 불필요 |

**globals.css 변경: 없음** (기존 Pretendard Variable CDN 유지)

**Stitch 폰트 사이즈/웨이트는 적용합니다** — 폰트 자체가 아닌 크기·굵기 조정은 Pretendard에도 동일 적용 가능합니다.

#### 폰트 사이즈 스케일

| 용도 | 기존 클래스 | Stitch 값 |
|------|------------|-----------|
| 페이지 타이틀 (h1) | `text-2xl` | `text-[1.875rem]` (30px), `font-bold`, `tracking-tight` |
| 모달 타이틀 (h2) | `text-xl` | `text-2xl` (24px), `font-semibold` |
| 카드 타이틀 | `text-sm font-semibold` | `font-bold text-slate-800` |
| 라벨 | `text-xs` | `text-xs uppercase tracking-wider` |
| 본문 | `text-sm` | `text-[15px] font-medium` |

### 2.3 Border Radius 변경

| 요소 | 기존 | Stitch |
|------|------|--------|
| 로그인 카드 | `rounded-3xl` | `rounded-[2rem]` (32px) |
| 모달 | `rounded-2xl` | `rounded-2xl` (동일) |
| 문서 카드 | `rounded-xl` | `rounded-lg` (12px) |
| 버튼 | `rounded-xl` | `rounded-lg` ~ `rounded-xl` |
| 프로그레스 바 | 없음 | `rounded-full` |

### 2.4 그림자 토큰 추가

```css
/* Stitch 신규 그림자 */
--shadow-luxury: 0 4px 20px rgba(22, 67, 156, 0.04);
--shadow-card-hover: 0 10px 30px rgba(22, 67, 156, 0.08);
--shadow-login: 0 20px 40px -10px rgba(0, 0, 0, 0.05);
```

### 2.5 아이콘 시스템 변경

| 기존 | Stitch | 조치 |
|------|--------|------|
| 인라인 SVG 직접 작성 | Material Symbols Outlined + Material Icons | Google Fonts 아이콘 링크 추가 |

**layout.tsx `<head>`에 추가:**
```html
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL@20..48,100..700,0..1" rel="stylesheet" />
```

**주요 아이콘 매핑:**

| 용도 | 아이콘명 | 사용 화면 |
|------|----------|-----------|
| 앱 로고 | `auto_awesome` | 로그인, 대시보드, 채팅 |
| PDF 문서 | `picture_as_pdf` | 카드, 사이드바 |
| 업로드 | `cloud_upload` | 업로드 모달 |
| 채팅 | `chat_bubble` | 카드 버튼, 사이드바 |
| 삭제 | `delete_outline` | 파일 삭제 |
| 더보기 | `more_vert` | 카드 옵션 |
| 라이트모드 | `light_mode` | 테마 토글 |
| 로그아웃 | `logout` | 헤더 |
| 보안 | `lock` / `security` | 업로드 모달 |
| 전송 | `arrow_upward` | 채팅 입력 |
| 복사 | `content_copy` | 메시지 복사 |
| 성공 | `check` / `check_circle` | 완료 상태 |
| 폴더 | `folder_open` | 빈 상태 |
| 추가 | `add` | 새 문서/세션 |

---

## 3. 화면별 작업 명세

### 3.1 S1 — 로그인 페이지 (Light)

**파일:** `src/app/login/page.tsx`

#### 현재 → Stitch 변경점

| 요소 | 현재 | Stitch | 변경 내용 |
|------|------|--------|-----------|
| 배경 | 그라데이션 오버레이 | `bg-[#faf9f7]` + 라디얼 그라데이션 (우측 상단) | 배경색 단순화, 라디얼 그라데이션 추가 |
| 앱 아이콘 | 인라인 SVG 문서 아이콘 | Material `auto_awesome` (32px) in 네이비(`#16439c`) 둥근 사각형 | 아이콘 교체 + 컨테이너 변경 |
| 타이틀 | `"Document Chat"` h1 | 동일 — `text-[1.875rem] font-bold tracking-tight` | 폰트 사이즈/웨이트 조정 |
| 서브타이틀 | `"PDF 문서와 AI 대화를..."` | `"PDF 문서와 AI 대화를 시작하세요"` | 텍스트 미세 수정 |
| Google 버튼 | 커스텀 SVG + 텍스트 | 동일 구조, `rounded-xl` + `border-gray-100/50` + 소프트 그림자 | 스타일 미세 조정 |
| 법적 링크 | `개인정보처리방침 \| 이용약관` | 동일 — `text-[#999] text-sm` | 색상 조정 |
| 카드 크기 | `max-w-md` (448px) | `max-w-[480px]` | 너비 소폭 증가 |
| 카드 모서리 | `rounded-3xl` | `rounded-[2rem]` | 동일 수준 |

#### 구현 포인트

```
1. 배경: bg-[#faf9f7] 위에 radial-gradient 오버레이 (우측 상단, 40% opacity)
2. 카드: max-w-[480px], rounded-[2rem], shadow-[0_20px_40px_-10px_rgba(0,0,0,0.05)]
3. 아이콘 영역: w-16 h-16 bg-[#16439c] rounded-2xl flex items-center justify-center
   - 내부: <span class="material-symbols-outlined text-white text-[32px]">auto_awesome</span>
4. Google 버튼: border border-gray-100/50 hover:shadow-md transition-all duration-200
5. 하단 링크: 개인정보처리방침 | 이용약관 (text-[#999999])
```

---

### 3.2 S2 — 대시보드 (문서 있음, Light)

**파일:** `src/features/pdf/CollectionListView.tsx`, `src/features/home/HomeClient.tsx`

#### 현재 → Stitch 변경점

| 요소 | 현재 | Stitch |
|------|------|--------|
| 레이아웃 | 단일 카드 내 리스트 (max-w-2xl) | 전체 폭 그리드 (max-w-[1200px]) |
| 상단 네비게이션 | 카드 내부 헤더 | sticky top-0 독립 네비바, backdrop-blur |
| 문서 표시 | 리스트 아이템 (행 단위) | **카드 그리드** (4열 반응형) |
| 카드 정보 | 제목, 업로드일 | 제목, 업로드일, **PAGES**, **CHUNKS** 메타데이터 |
| 카드 액션 | 채팅/삭제 버튼 | `채팅 시작` 버튼 + `more_vert` 옵션 메뉴 |
| PDF 아이콘 | 작은 SVG | `picture_as_pdf` Material Icon (색상별 구분) |
| 프로필 | 이니셜 아바타 (작음) | 이니셜 원형 아바타 + 이름 텍스트 |
| 테마 토글 | 3단 (light/dark/system) | 1버튼 토글 (`light_mode` 아이콘) |
| 업로드 버튼 | 하단 텍스트 버튼 | 우측 상단 강조 버튼 (`+ PDF 업로드`) |

#### 핵심 구조 변경

```
<네비게이션 바> (sticky, backdrop-blur)
  좌: 앱 로고(auto_awesome) + "Document Chat"
  우: 프로필 아바타 + 이름 + 테마 토글 + 로그아웃

<메인 콘텐츠> (max-w-[1200px] mx-auto px-6 py-12)
  <헤더>
    좌: "내 문서" h1 + "RAG 데이터베이스로..." 서브텍스트
    우: "+ PDF 업로드" 버튼 (bg-[#16439c] text-white rounded-xl shadow-lg)

  <문서 그리드> (grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6)
    <카드> (bg-white rounded-lg p-5 border hover 시 translateY(-4px) + shadow 증가)
      - PDF 아이콘 (w-12 h-12 bg-primary/5 rounded-lg)
      - 제목 (font-bold text-slate-800, truncate)
      - 업로드일 (text-xs text-slate-400)
      - 구분선 (border-t)
      - 메타: PAGES / CHUNKS (text-xs uppercase tracking-wider)
      - "채팅 시작" 버튼 (w-full bg-primary/5 hover:bg-primary/10)

  <푸터> "전체 N개 문서" (text-sm text-center text-slate-400)
```

#### 반응형 브레이크포인트

| 화면 | 열 수 |
|------|-------|
| < 768px | 1열 |
| 768px ~ 1023px | 2열 |
| 1024px ~ 1279px | 3열 |
| ≥ 1280px | 4열 |

---

### 3.3 S3 — 대시보드 빈 상태 (Light)

**파일:** `src/features/home/HomeClient.tsx` (빈 상태 분기)

#### Stitch 디자인

```
<동일 네비게이션 바>

<빈 상태 콘텐츠> (flex items-center justify-center min-h-[calc(100vh-80px)])
  - 큰 원형 아이콘 (w-40 h-40, 연한 그라데이션 배경)
    - folder_open 아이콘 (8xl, opacity-20)
    - 우하단 + 아이콘 뱃지 (w-10 h-10 bg-primary text-white rounded-full)
  - "업로드된 문서가 없습니다" (text-3xl font-bold)
  - "PDF 문서를 업로드하여 / AI와 대화를 시작해보세요." (text-slate-400)
  - "+ PDF 업로드" 버튼 (bg-primary text-white px-8 py-4 rounded-2xl shadow-xl)
    - hover 시 -translate-y-1 효과
```

---

### 3.4 S4 ~ S7 — PDF 업로드 모달 (Dark, 4단계)

**파일:** `src/features/pdf/PdfUploadModalView.tsx`, `src/features/pdf/PdfUploadView.tsx`

#### 단계별 상태 흐름

```
S4 (드래그&드롭) → S5 (파일 선택됨) → S6 (업로드 중) → S7 (완료)
```

#### S4 — 드래그&드롭 상태

```
<모달 오버레이> (fixed inset-0, bg-black/70 backdrop-blur)
  <배경 장식> (블러 처리된 원형 그라데이션 2개 — primary/20, indigo-500/20)

  <모달 카드> (max-w-xl, bg-gradient-to-br from-[#0f1219] to-[#1a202e],
              rounded-2xl, border border-white/10, shadow-2xl)

    <헤더> (px-8 pt-8 pb-4)
      좌: "PDF 업로드" (text-2xl font-semibold tracking-tight text-white)
      좌하: "분석할 문서를 업로드하여..." (text-sm text-gray-400 font-light)
      우: X 닫기 버튼

    <업로드 영역> (px-8 pb-6)
      <드롭존> (border-2 border-dashed border-white/10 rounded-xl p-6
               bg-white/[0.02] hover:bg-white/[0.04] hover:border-primary/50)
        - cloud_upload 아이콘 (w-16 h-16, bg-primary/10 rounded-full)
          hover 시 scale-110
        - "PDF 파일을 드래그하거나 클릭하여 선택하세요" (text-white)
        - "최대 50MB, PDF 형식만 지원됩니다" (text-gray-500 text-xs)
        - "파일 선택하기" 버튼 (bg-white text-[#111621] rounded-lg)

    <푸터> (px-8 pb-6, border-t border-white/5)
      - 🔒 보안 암호화 전송 + ⚡ 빠른 AI 분석 (text-gray-500 text-xs)
```

#### S5 — 파일 선택됨 상태

```
<동일 모달 오버레이 + 배경>

  <모달 카드> (bg-[#0a0f18], border border-[#233044], rounded-xl)
    <헤더> "PDF 업로드" + X 버튼 (border-b 구분)

    <콘텐츠>
      - "파일이 선택되었습니다" (text-xl font-medium text-[#ecebe4])
      - "분석을 시작하려면 업로드 버튼을 눌러주세요." (text-sm text-gray-400)

      <파일 아이템> (bg-[#1a2436], border-2 border-primary/50, rounded-lg p-6)
        좌: picture_as_pdf 아이콘 (text-red-400)
        중: 파일명 + 파일 크기 (text-[#ecebe4] / text-gray-400)
        우상: ✓ 체크 뱃지 (bg-primary text-white rounded-full)
        우: 🗑 삭제 버튼 (hover:text-red-400)

      - 🔒 "모든 문서는 암호화되어 안전하게 처리됩니다." (text-xs)

    <푸터> (border-t)
      - "취소" 텍스트 버튼 + "업로드" Primary 버튼 (bg-primary text-white)
```

#### S6 — 업로드/임베딩 진행중

```
<모달 카드> (bg-[#1e2330], border border-[#2d3342], rounded-xl)
  <헤더> "PDF 업로드" + X 버튼

  <콘텐츠> (p-8 text-center)
    - cloud_upload 아이콘 (animate-pulse, bg-slate-800 rounded-full)
    - 🔄 스피너 + "업로드 및 임베딩 처리 중..." (text-lg font-semibold text-white)
    - "잠시만 기다려주세요. 문서가 분석되고 있습니다." (text-sm text-gray-400)

    <프로그레스 바 영역>
      - "진행률" 라벨 + "65%" 퍼센트 (flex justify-between)
      - <프로그레스 바> (h-2.5 bg-[#2d3342] rounded-full)
        - <바 채움> (bg-[#5b7a9d] rounded-full, width: N%)
        - <시머 효과> (animate-[shimmer_1.5s_infinite], via-white/20)

  <푸터> "취소" 텍스트 버튼
```

#### S7 — 임베딩 완료

```
<모달 카드> (bg-[#1e293b], border border-gray-700, rounded-2xl)

  <콘텐츠> (p-8 text-center)
    - ✓ 체크 아이콘 (w-20 h-20, bg-[#10b981] text-white rounded-full, shadow-lg)
    - "임베딩 완료!" (text-2xl font-bold text-white)
    - "문서 분석이 성공적으로 완료되었습니다. / 이제 AI와 대화를 시작해보세요."

    <파일 정보> (bg-gray-800/50, border border-gray-700, rounded-xl p-4)
      좌: picture_as_pdf 아이콘 (bg-red-500/10, text-red-400)
      중: 파일명 + "1.2 MB · 처리 완료"
      우: check_circle (text-[#10b981])

    - "채팅 시작" Primary 버튼 (w-full bg-primary text-white py-3.5 rounded-xl shadow-lg)
    - "나중에 하기" 텍스트 링크

  <구분선 후 하단> 없음 (모달에서 바로 대시보드로 이동 가능)
```

---

### 3.5 S8 — 채팅 페이지 (Light)

**파일:** `src/features/chat/` 전체

#### 레이아웃 구조

```
<전체 레이아웃> (flex h-screen)

  <사이드바> (w-72, bg-[#f2f0eb], border-r border-[#e5e2da])
    <헤더>
      - "새로운 세션" 버튼 (w-full, bg-white border border-[#e5e2da])

    <문서 목록> (space-y-1)
      - PDF 항목들 (px-4 py-3, hover:bg-white/60, rounded-xl)
        좌: description 아이콘 (text-slate-400)
        중: 파일명 (text-slate-700 font-medium truncate)

    <채팅 히스토리>
      - 라벨: "최근 대화" (text-[10px] font-bold uppercase tracking-[0.2em])
      - 세션 항목들 (px-4 py-3, hover:bg-white/40, rounded-xl)
        좌: chat_bubble_outline 아이콘
        중: 세션 제목 (truncate)

    <푸터>
      - 유저 프로필 (아바타 + 이름)
      - light_mode 토글 + logout 버튼

  <메인 영역> (flex-1 flex flex-col, bg-[#faf9f7])
    <채팅 헤더> (px-8 py-4, border-b border-[#e5e2da])
      좌: 세션 제목 (text-lg font-bold)
      우: PDF 문서 인디케이터 뱃지들

    <메시지 영역> (flex-1 overflow-y-auto, px-4 lg:px-0 max-w-3xl mx-auto)
      <메시지 목록> (space-y-8)

        <유저 메시지> (flex justify-end)
          <버블> (bg-[#1a1f2e] text-white px-5 py-3.5 rounded-2xl max-w-[85%])

        <AI 메시지> (flex items-start gap-4)
          <아바타> (w-10 h-10, bg-white rounded-xl, smart_toy 아이콘)
          <버블> (bg-[#f8f6f3] border border-[#e5e2da] px-6 py-5 rounded-2xl max-w-[90%])
            - NexTalk AI 분석 (text-sm font-bold)
            - 마크다운 콘텐츠 (prose: 리스트, 볼드, 코드블록)
            - <소스 참조> (flex gap-2)
              뱃지: (bg-white border rounded-full px-2.5 py-1 text-[10px])

    <입력 영역> (px-8 py-6, border-t border-[#e5e2da])
      <파일 인디케이터> (bg-white border rounded-xl px-4 py-2, 파일명 + 페이지수)
      <입력 바> (flex bg-white border border-[#e5e2da] rounded-2xl px-5 py-3)
        좌: 텍스트 입력 (flex-1)
        우: 전송 버튼 (w-10 h-10 bg-[#1a1f2e] rounded-xl, arrow_upward)
      <면책 문구> (text-xs text-center text-slate-400)
```

#### 채팅 사이드바 텍스처

```css
/* 사이드바 배경 도트 텍스처 */
background-image: radial-gradient(#d1cdc2 0.5px, transparent 0.5px);
background-size: 24px 24px;
```

#### 커스텀 스크롤바

```css
/* 사이드바 스크롤바 */
scrollbar-width: thin;
scrollbar-color: #e5e2da transparent;
/* Webkit */
::-webkit-scrollbar { width: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #e5e2da; border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: #d1cdc2; }
```

---

## 4. Stitch 미포함 — 기존 기능 보존 목록

> Stitch 디자인 8개 화면에는 **없지만** 프로젝트에 이미 구현된 기능들입니다.
> Stitch 스타일을 적용할 때 이 기능들이 삭제되거나 깨지지 않도록 반드시 보존해야 합니다.

### 4.1 사용자 설정 패널

**현재 위치:** `ChatSidebar.tsx` 하단 ⚙️ 아이콘, `CollectionListView.tsx` 헤더 ⚙️ 아이콘

**포함 기능:**

| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 테마 토글 | Light / Dark / System 3단 전환 | ✅ 구현 완료 |
| 회원 탈퇴 | "탈퇴합니다" 입력 확인 → 30일 유예 소프트 삭제 | ✅ 구현 완료 |
| 로그아웃 | Supabase 세션 종료 → /login 리다이렉트 | ✅ 구현 완료 |

**현재 UI 구조:**
```
<설정 패널> (absolute, bottom-full, w-56, bg-paper, shadow-lg, 애니메이션)
  ├── 테마 토글 (아이콘 + "테마" 라벨 + 현재 모드 텍스트)
  ├── 구분선
  ├── 회원 탈퇴 (아이콘 + "회원 탈퇴" 텍스트)
  ├── 구분선
  └── 로그아웃 (아이콘 + "로그아웃" 텍스트)
```

**Stitch 적용 시 조치:**
- 패널 자체의 **기능과 위치를 유지**하면서, 패널 내부 스타일만 Stitch 톤으로 조정
- `settings-panel`, `settings-panel-item`, `settings-panel-icon` CSS 클래스 보존
- ⚙️ 아이콘의 90° 회전 애니메이션 보존

### 4.2 회원 탈퇴 모달 (`AccountDeleteModal.tsx`)

**현재 UI:**
```
<모달 오버레이> (rgba(26,31,46,0.7), fixed)
  <모달 카드> (rounded-3xl, max-w-md, bg-paper, shadow-xl)
    - ⚠️ 경고 아이콘 (error-bg 배경)
    - "정말 탈퇴하시겠습니까?" 제목
    - 경고 박스 (삭제 대상 데이터 목록)
    - "탈퇴합니다" 입력 필드 (focus-ring)
    - 취소 / 탈퇴 버튼
```

**Stitch 적용 시 조치:** 그대로 보존 (Stitch에 해당 디자인 없음)

### 4.3 탈퇴 완료 페이지 (`/account/deleted`)

**기능:** 소프트 삭제 후 표시, "새로 시작하기" 또는 "로그아웃" 선택

**Stitch 적용 시 조치:** 그대로 보존

### 4.4 크레딧 잔액 표시

**현재 위치:** `CollectionListView.tsx` 헤더 (유저 이름 옆)

**표시:** `"잔액 $X.XX"` (로딩 시 스켈레톤 애니메이션)

**Stitch 적용 시 조치:**
- 대시보드 헤더를 Stitch의 sticky 네비바로 바꿀 때, 잔액 표시를 네비바 우측에 통합
- `useCredits()` 훅 연결 유지

### 4.5 크레딧 차단 오버레이 (`CreditBlockOverlay.tsx`)

**트리거:** 잔액 ≤ $0.20 시 전체 화면 오버레이 표시

**Stitch 적용 시 조치:** 그대로 보존

### 4.6 보존해야 할 상태 관리

| Store/Hook | 용도 | Stitch 작업 시 주의 |
|------------|------|---------------------|
| `creditsStore` | 잔액, 차단 상태 | CollectionListView 리디자인 시 `useCredits()` 연결 유지 |
| `themeStore` | 테마 3단 토글 | 설정 패널 스타일 변경 시 토글 로직 유지 |
| `isSettingsOpen` 상태 | 설정 패널 열기/닫기 | ChatSidebar, CollectionListView 둘 다 보존 |
| `isDeleteModalOpen` 상태 | 탈퇴 모달 열기/닫기 | 설정 패널 내 "회원 탈퇴" 클릭 시 연결 유지 |
| `settingsRef` | 패널 외부 클릭 감지 | ref 바인딩 유지 |

---

## 5. 수정 대상 파일 매핑

### 5.1 필수 수정 파일

| 파일 | 변경 유형 | 영향 범위 | 우선순위 |
|------|-----------|-----------|----------|
| `src/app/globals.css` | 디자인 토큰 추가/수정 | 전역 | 🔴 1순위 |
| `src/app/layout.tsx` | 폰트 임포트, 아이콘 링크, 메타데이터 | 전역 | 🔴 1순위 |
| `src/app/login/page.tsx` | 전체 UI 리디자인 | 로그인 | 🔴 1순위 |
| `src/features/pdf/CollectionListView.tsx` | 리스트 → 그리드 카드 전환 ⚠️ 설정 패널·크레딧 잔액 보존 | 대시보드 | 🟡 2순위 |
| `src/features/home/HomeClient.tsx` | 빈 상태 + 네비바 추가 ⚠️ CreditBlockOverlay·AccountDeleteModal 보존 | 대시보드 | 🟡 2순위 |
| `src/features/pdf/PdfUploadModalView.tsx` | 4단계 모달 UI 리디자인 | 업로드 | 🟡 2순위 |
| `src/features/pdf/PdfUploadView.tsx` | 진행바 + 완료 화면 리디자인 | 업로드 | 🟡 2순위 |
| `src/features/chat/ChatSidebar.tsx` | 사이드바 전면 리디자인 ⚠️ 설정 패널(⚙️) 기능·상태 보존 | 채팅 | 🟢 3순위 |
| `src/features/chat/ChatLayout.tsx` | 레이아웃 구조 변경 | 채팅 | 🟢 3순위 |
| `src/features/chat/MessageListView.tsx` | 메시지 버블 스타일 변경 | 채팅 | 🟢 3순위 |
| `src/features/chat/MessageInputView.tsx` | 입력 영역 리디자인 | 채팅 | 🟢 3순위 |
| `src/shared/components/ThemeToggleButton.tsx` | 단순 토글로 변경 | 공통 | 🟢 3순위 |

### 5.2 보존 필수 파일 (수정 불가)

> Stitch 디자인에 포함되지 않은 기존 기능 파일들입니다. 리디자인 시 이 파일들의 import/연결을 끊지 마세요.

| 파일 | 기능 | 보존 이유 |
|------|------|-----------|
| `src/shared/components/AccountDeleteModal.tsx` | 회원 탈퇴 확인 모달 | 법적 문서(약관·개인정보처리방침)에 명시된 기능 |
| `src/app/account/deleted/page.tsx` | 탈퇴 완료 페이지 | 소프트 삭제 → 재활성화 UX |
| `src/shared/components/CreditBlockOverlay.tsx` | 크레딧 부족 차단 오버레이 | 과금 정책 필수 |
| `src/shared/lib/stores/creditsStore.ts` | 크레딧 상태 관리 | CollectionListView에서 잔액 표시용 |
| `src/shared/hooks/useCredits.ts` | 크레딧 훅 | 잔액 로딩·표시 로직 |

### 5.3 신규 생성 파일 (필요 시)

| 파일 | 용도 |
|------|------|
| `src/shared/components/NavigationBar.tsx` | 대시보드 공통 네비게이션 바 |
| `src/shared/components/DocumentCard.tsx` | 문서 카드 컴포넌트 (그리드용) |
| `src/shared/components/ProgressBar.tsx` | 업로드 프로그레스 바 |

---

## 6. 작업 순서 및 우선순위

### Phase 1 — 디자인 토큰 기반 (Day 1)

```
작업 1-1: globals.css 디자인 토큰 업데이트
  - 신규 CSS 변수 추가 (primary, border-light, sidebar-bg 등)
  - 다크 모드 변수 수정 (cream, paper 값 변경)
  - 그림자 토큰 추가
  - 신규 유틸리티 클래스 추가 (shadow-luxury 등)

작업 1-2: layout.tsx 전역 설정
  - Material Icons/Symbols 링크 추가
  - 메타데이터 title "Document Chat", description 수정
  - 폰트: Pretendard Variable 유지 (변경 없음)

작업 1-3: 공통 컴포넌트 생성
  - NavigationBar.tsx (sticky 네비바)
  - DocumentCard.tsx (문서 카드)
  - ProgressBar.tsx (프로그레스 바 + 시머 효과)
```

### Phase 2 — 로그인 + 대시보드 (Day 2)

```
작업 2-1: login/page.tsx 리디자인
  - 배경, 카드, 아이콘, 버튼 스타일 Stitch 디자인 적용
  - Material Icons 적용 (auto_awesome)

작업 2-2: CollectionListView.tsx 리디자인
  - 리스트 → 그리드 카드 레이아웃 전환
  - PAGES/CHUNKS 메타데이터 표시
  - DocumentCard 컴포넌트 활용
  - more_vert 옵션 메뉴 추가
  - ⚠️ 보존: 크레딧 잔액 표시 (useCredits 훅) → 네비바 우측에 통합
  - ⚠️ 보존: 설정 패널 (⚙️ 아이콘, isSettingsOpen, settingsRef) 유지
  - ⚠️ 보존: animate-float-in 등장 애니메이션

작업 2-3: HomeClient.tsx 리디자인
  - NavigationBar 적용
  - 빈 상태 UI (큰 폴더 아이콘 + CTA)
  - 문서 있음 상태 연결
  - ⚠️ 보존: CreditBlockOverlay, AccountDeleteModal import 유지
```

### Phase 3 — 업로드 모달 (Day 3)

```
작업 3-1: PdfUploadModalView.tsx 4단계 리디자인
  - 드래그&드롭 상태 (다크 모달 + 블러 배경 장식)
  - 파일 선택됨 상태 (파일 정보 카드 + 체크 뱃지)
  - 업로드 중 상태 (프로그레스 바 + 시머 애니메이션)
  - 완료 상태 (성공 체크마크 + 채팅 시작 CTA)

작업 3-2: PdfUploadView.tsx 진행 상태 연동
  - ProgressBar 컴포넌트 연결
  - 단계별 상태 전환 로직 확인
```

### Phase 4 — 채팅 페이지 (Day 4~5)

```
작업 4-1: ChatSidebar.tsx 전면 리디자인
  - 배경 텍스처 (도트 패턴)
  - 문서 목록 + 채팅 히스토리 분리
  - "새로운 세션" 버튼
  - 커스텀 스크롤바
  - 하단 프로필 + 토글 + 로그아웃
  - ⚠️ 보존: 설정 패널 (isSettingsOpen, isDeletingAccount, isDeleteModalOpen 상태)
  - ⚠️ 보존: settingsRef 외부 클릭 감지 (useEffect)
  - ⚠️ 보존: AccountDeleteModal 연결 (회원 탈퇴 트리거)

작업 4-2: ChatLayout.tsx 구조 변경
  - 사이드바 너비 w-72 (288px)
  - 헤더 구조 변경 (세션 제목 + PDF 뱃지)
  - border-[#e5e2da] 구분선 적용

작업 4-3: MessageListView.tsx 메시지 스타일
  - 유저 버블: bg-[#1a1f2e], rounded-2xl
  - AI 버블: bg-[#f8f6f3], border, AI 아바타 추가
  - 소스 참조 뱃지 (rounded-full, text-[10px])

작업 4-4: MessageInputView.tsx 입력 영역
  - 파일 인디케이터 뱃지
  - 입력 바 스타일 (rounded-2xl, border)
  - 전송 버튼 (bg-[#1a1f2e], arrow_upward)
  - 면책 문구 스타일
```

### Phase 5 — 마무리 (Day 5~6)

```
작업 5-1: ThemeToggleButton.tsx 단순화
  - 3단 토글 → 1버튼 토글 (light_mode Material Icon)

작업 5-2: 다크 모드 전체 검증
  - 모든 화면에서 다크 모드 동작 확인
  - 신규 CSS 변수의 다크 모드 대응 확인

작업 5-3: 반응형 테스트
  - 모바일 (< 768px): 사이드바 오버레이, 1열 그리드
  - 태블릿 (768~1024px): 2열 그리드
  - 데스크톱 (≥ 1280px): 4열 그리드, 고정 사이드바

작업 5-4: 애니메이션 QA
  - 카드 호버 효과 (translateY, shadow 전환)
  - 모달 진입/퇴장 애니메이션
  - 프로그레스 바 시머 효과
  - 빈 상태 아이콘 회전 (hover:rotate-90)
```

---

## 7. 주의사항 및 호환성 체크

### 7.1 기존 기능 보존

| 기능 | 확인 사항 |
|------|-----------|
| Google OAuth 로그인 | `handleGoogleLogin()` 함수 그대로 유지 |
| PDF 업로드 로직 | `PdfUploadModalContainer.tsx`의 상태 관리 그대로 유지 |
| 채팅 API 연동 | `chatStore`, `ChatContainer` 비즈니스 로직 변경 없음 |
| 라우팅 | `/login`, `/`, `/chat/[pdfId]` 경로 유지 |
| 미들웨어 인증 | `/privacy`, `/terms` 면제 유지 |
| **설정 패널** | `isSettingsOpen`, `settingsRef`, 외부 클릭 감지 로직 보존 (섹션 4.1 참조) |
| **회원 탈퇴** | `AccountDeleteModal` import 및 `isDeleteModalOpen` 상태 연결 유지 |
| **크레딧 표시** | `useCredits()` 훅 연결, 잔액 표시, `CreditBlockOverlay` 보존 |
| **테마 토글** | `themeStore` 3단 토글 (light/dark/system) 로직 유지 |

### 7.2 Stitch 코드 직접 사용 시 주의

Stitch가 생성한 HTML은 **정적 프로토타입**입니다. 다음 사항에 유의하세요:

- Stitch HTML의 인라인 스타일/CDN 링크는 참조만 하고, Tailwind 클래스로 변환
- Stitch의 하드코딩된 텍스트/데이터는 동적 바인딩으로 교체
- Stitch의 이벤트 핸들러(onclick 등)는 React 이벤트로 교체
- 폰트는 Stitch CDN이 아닌 Next.js `next/font` 또는 layout.tsx에서 로드

### 7.3 성능 고려

| 항목 | 권장 사항 |
|------|-----------|
| Google Fonts 로딩 | `display=swap` 파라미터 포함, `preconnect` 힌트 추가 |
| Material Icons | 번들 사이즈 큼 → 자주 쓰는 아이콘만 SVG로 직접 포함 고려 |
| 배경 블러 효과 | `backdrop-blur` 모바일 성능 저하 가능 → 모바일에서 제거 검토 |
| 카드 그리드 | 문서 100개 이상 시 가상화(virtualization) 검토 |
| 프로그레스 시머 | CSS 애니메이션만 사용 (JS 불필요) |

### 7.4 접근성 유지

| 항목 | 확인 사항 |
|------|-----------|
| 키보드 네비게이션 | 모든 버튼/링크 Tab 접근 가능 |
| ARIA 라벨 | Material Icon에 `aria-label` 또는 `aria-hidden="true"` 적용 |
| 색상 대비 | Primary (#16439c) 위의 흰색 텍스트 → 대비비 7.2:1 ✅ |
| 포커스 링 | `focus:ring-2 focus:ring-primary/20 focus:ring-offset-2` 유지 |

---

## 8. 백엔드 영향 분석

### 8.1 결론: 백엔드/스키마 변경 불필요

Stitch 디자인의 모든 UI 요소에 대해 기존 코드를 분석한 결과,
**데이터베이스 스키마 변경이나 신규 API 엔드포인트 생성 없이 프론트엔드만 수정**하면 됩니다.

### 8.2 Stitch 신규 UI 요소별 데이터 출처

| Stitch 신규 UI 요소 | 데이터 필요 | 기존 데이터 존재 여부 | 출처 |
|---------------------|------------|---------------------|------|
| 대시보드 카드 — CHUNKS 수 | chunk 수 | ✅ 이미 구현됨 | `CollectionInfo.documentCount` (RPC `get_user_collections`) |
| 대시보드 카드 — PAGES 수 | 페이지 수 | ✅ DB에 존재 | `pdf_documents.total_pages` (스키마에 이미 있음) |
| 사이드바 — PDF 문서 목록 | PDF 리스트 | ✅ 이미 구현됨 | `usePdfStore.collections` → `ChatSidebar.tsx` 라인 99~137 |
| 사이드바 — 최근 대화 히스토리 | 세션 목록 | ✅ 이미 구현됨 | `useChatStore.sessionList` → `ChatSidebar.tsx` 라인 139~182 |
| 업로드 완료 — 파일 크기 | fileSize | ✅ 타입 정의됨 | `UploadPdfResponse.fileSize` (현재 0 하드코딩 → File 객체에서 추출) |
| 업로드 완료 — "처리 완료" 상태 | status | ✅ 이미 구현됨 | `SavePdfResponse.status === 'embedded'` |
| 채팅 — 소스 참조 뱃지 (PDF명) | fileName | ✅ 이미 구현됨 | `ChatSource.fileName` → `MessageListView.tsx` 라인 478 |
| 채팅 — 소스 참조 뱃지 (페이지) | pageNumber | ✅ 이미 구현됨 | `ChatSource.pageNumber` → `MessageListView.tsx` 라인 472 |

### 8.3 프론트엔드에서만 처리 가능한 이유

1. **기본 프로필 데이터** (이름, 아바타): Supabase Auth `user` 객체에서 이미 접근 가능
2. **문서 메타데이터** (제목, 날짜, 청크수): `get_user_collections` RPC가 이미 반환
3. **채팅 세션 목록**: `/api/chat/sessions` API가 이미 `last_message_at` 기준 정렬 반환
4. **소스 참조**: `chat_messages.sources` JSONB 컬럼에 이미 `fileName`, `pageNumber` 저장
5. **업로드 상태**: `SavePdfResponse`에 `status`, `totalPages` 이미 정의

### 8.4 선택적 개선 (필수 아님)

| 항목 | 현재 상태 | 개선 방법 | 필수 여부 |
|------|-----------|-----------|-----------|
| PAGES 수 표시 | RPC에서 미반환 | `get_user_collections` RPC에 `total_pages` 필드 추가 | 선택 (없으면 UI에서 숨김 처리 가능) |
| 파일 크기 표시 | `fileSize: 0` 하드코딩 | `upload/route.ts`에서 `file.size` 값 할당 | 선택 (1줄 수정) |

> 위 2건은 **프론트엔드 타입 수정 + 기존 API 응답 값 채우기** 수준이며,
> 데이터베이스 마이그레이션이나 신규 테이블/컬럼 생성은 필요하지 않습니다.

---

## 9. 작업 성공률 평가

### 9.1 종합 성공률: 92%

| 평가 기준 | 점수 | 근거 |
|-----------|------|------|
| **디자인 충실도** | 95% | Stitch 8개 화면 모두 Tailwind CSS로 변환 가능, 폰트 차이는 의도적 결정 |
| **백엔드 호환성** | 98% | 스키마 변경 0건, API 변경 0건, 타입 확장 2건(선택) |
| **기존 기능 보존** | 95% | OAuth, PDF 업로드, 채팅 API, 라우팅 모두 비즈니스 로직 변경 없음 |
| **기술적 실현 가능성** | 90% | 대부분 CSS/JSX 수정, 대시보드 그리드 전환이 가장 큰 구조 변경 |
| **일정 준수 가능성** | 85% | 5~6일 예상, 채팅 사이드바 리디자인이 가장 복잡 |

### 9.2 화면별 성공률 세부 평가

| 화면 | 난이도 | 성공률 | 리스크 요인 |
|------|--------|--------|-------------|
| S1 로그인 | 낮음 | **98%** | 단순 스타일 변경, 기능 변경 없음 |
| S2 대시보드 (문서 있음) | 중간 | **90%** | 리스트→그리드 전환, 반응형 브레이크포인트 QA 필요 |
| S3 대시보드 (빈 상태) | 낮음 | **98%** | 신규 빈 상태 UI 추가, 기존 분기에 삽입 |
| S4 업로드 모달 (드래그&드롭) | 중간 | **92%** | 다크 모달 스타일, 블러 배경 장식 추가 |
| S5 업로드 모달 (파일 선택) | 중간 | **92%** | 기존 상태와 매핑, 파일 크기 표시 (1줄 수정) |
| S6 업로드 모달 (진행중) | 중간 | **90%** | 프로그레스 바 + 시머 애니메이션 신규 구현 |
| S7 업로드 완료 | 중간 | **92%** | 성공 모달 신규, 채팅 시작 라우팅 연결 |
| S8 채팅 페이지 | 높음 | **85%** | 사이드바 전면 리디자인, 도트 텍스처, 스크롤바 커스텀 |

### 9.3 리스크가 가장 높은 작업 Top 3

| 순위 | 작업 | 리스크 | 대응 방안 |
|------|------|--------|-----------|
| 1 | 채팅 사이드바 전면 리디자인 | 기존 상태 관리 로직과 새 UI의 연결 복잡도 | `ChatSidebar.tsx`의 기존 props/hooks 유지하며 JSX만 교체 |
| 2 | 대시보드 리스트→그리드 전환 | 기존 `CollectionListView`의 이벤트 핸들러가 새 카드 구조에 맞는지 | 기존 `onChatStart`, `onDelete` 콜백을 카드 컴포넌트에 전달 |
| 3 | 업로드 모달 4단계 분리 | 기존 2단계(업로드/저장)를 4단계 UI로 매핑 | `PdfUploadModalContainer`의 상태 머신 확장 없이 기존 상태에 UI 분기 추가 |

### 9.4 성공률을 높이기 위한 권장 사항

1. **Phase 1(토큰/전역)을 먼저 완료한 후 나머지 진행** — 디자인 토큰이 잘못되면 모든 화면에 영향
2. **각 Phase 완료 후 기존 기능 회귀 테스트** — OAuth 로그인, PDF 업로드, 채팅 기능 정상 동작 확인
3. **채팅 페이지(Phase 4)는 마지막에 진행** — 가장 복잡하므로 앞단 작업으로 패턴 익힌 후 진행
4. **Stitch HTML 코드를 복붙하지 말고 참고만** — React/Tailwind로 변환하며 기존 로직 보존

---

> **참고:** 각 Stitch HTML 원본 코드는 `stitch_desktop_login_screen/` 폴더에서 확인 가능합니다.
> 스크린샷(`screen.png`)과 코드(`code.html`)를 나란히 비교하며 작업하세요.
